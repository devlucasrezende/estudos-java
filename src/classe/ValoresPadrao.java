package classe;

public class ValoresPadrao {
	public static void main(String[] args) {
		
		
		// byte, short, int, long -> 0 (padrão)
		// float, double -> 0.0 (padrão)
		// boolean -> false
		// char -> '\u0000'
		// objetos -> null
		
		
		//int a; 
		//System.out.println(a); // não compila pois uma variavel local não é inicializada por padrão! Uma variável não inicializada dentro da instância = 0;
		
		//final int x; // quando for uma variavel imutavel (final) deve ser inicializado até no máximo o construtor rodar (criação do objeto), ou dentro do próprio construtor;
		
	}

}
